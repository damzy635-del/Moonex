# Moonex

Moonex AI application.

## Development workflow

Production is kept on `main`. Active development is consolidated on `moonex-development`.

Changes should be tested on `moonex-development` before being merged into `main`.
